const SettingsPage = () => {
  return (
    <section className="rounded-lg border border-slate-800 bg-slate-900/80 p-6 shadow-soft">
      <h2 className="text-xl font-semibold text-white">Settings</h2>
      <p className="mt-2 text-sm text-slate-400">Authentication is active. Keep environment keys, database access, and admin account details up to date.</p>
    </section>
  );
};

export default SettingsPage;
